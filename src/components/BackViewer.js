import React from 'react';

function BackViewer() {
    // TODO: path variable (id) 받아서 등 이미지 받아오고, 화면에 등 이미지 그려야 함
    // TODO: ws open
    // TODO: image 위에 onClick listener 추가하고, 좌표를 normalize 해서 ws 호출

    return (
        <div>
            <h2>
                back viewer
            </h2>
        </div>
    )
}

export default BackViewer;
